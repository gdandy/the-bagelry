import { ContractNavBarDirective } from './contract-nav-bar.directive';

describe('ContractNavBarDirective', () => {
  it('should create an instance', () => {
    const directive = new ContractNavBarDirective();
    expect(directive).toBeTruthy();
  });
});
